enum ApiStatus {
  init,
  isLoading,
  success,
  failed,
}

enum ConnectionType {
  noInternet,
  mobile,
  wifi,
}

enum NavigationType {
  home,
  stats,
  profile,
}

enum SpeechType {
  init,
  isSpeeching,
  speechPaused,
  speechClosed,
}
